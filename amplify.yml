version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm use 22
        - npm ci --omit=dev
    build:
      commands:
        - npm run build:amplify
  artifacts:
    baseDirectory: .amplify-hosting
    files:
      - '**/*'
